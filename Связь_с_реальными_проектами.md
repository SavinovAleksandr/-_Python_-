# Связь учебных материалов с реальными проектами

Этот документ показывает, как учебные примеры связаны с реальным кодом из ваших проектов DSS и StabLimit.

## 🔗 Связь между учебными материалами и реальным кодом

### 1. COM-соединение с RasterWin

**Учебный пример**: `Примеры/пример_COM_соединение.py`

**Реальный код**:
- **DSS**: `python_dss/rastr_operations/rastr_operations.py` (строки 14-56)
  ```python
  self._rastr = win32com.client.Dispatch("Astra.Rastr")
  ```

- **StabLimit**: Аналогично используется `win32com.client.Dispatch`

**Что изучить**:
- Как подключаться к RasterWin
- Обработка ошибок подключения
- Проверка доступности COM на платформе

---

### 2. Запуск расчета установившегося режима (RGM)

**Учебный пример**: `Примеры/пример_COM_соединение.py` → функция `пример_расчета_режима()`

**Реальный код**:
- **StabLimit**: `RastrWinAPI/src/calculation/regim.py` (строки 22-61)
  ```python
  def rgm(rastr_win, par="p"):
      kod = rastr_win.rgm(par)
      return kod == 0
  ```

- **DSS**: `python_dss/rastr_operations/rastr_operations.py` (строки 176-193)
  ```python
  def rgm(self, param="", iterations=None, voltage=None):
      table = self._rastr.Tables.Item("com_regim")
      # ... настройка параметров ...
      return self._rastr.rgm(param) == 0
  ```

**Что изучить**:
- Метод `rastr.rgm()` с параметрами
- Проверка успешности расчета (код 0)
- Настройка параметров расчета через таблицы

---

### 3. Запуск динамического расчета

**Учебный пример**: `Примеры/пример_чтение_протокола.py` → функция `извлечь_результат_динамики()`

**Реальный код**:
- **DSS**: `python_dss/rastr_operations/rastr_operations.py` (строки 797-861)
  ```python
  def run_dynamic(self, ems=False, max_time=-1.0):
      fw_dynamic = self._rastr.FWDynamic()
      if ems:
          ret_code = fw_dynamic.RunEMSmode()
      else:
          ret_code = fw_dynamic.Run()
      result.is_stable = fw_dynamic.SyncLossCause == 0
  ```

- **StabLimit**: `RastrWinAPI/src/calculation/dynamic.py` (строки 13-103, 106-204)
  ```python
  def run(rastr_win):
      spFWDynamic = rastr_win.FWDynamic()
      resultAST_OK = spFWDynamic.Run()
      system_stable = spFWDynamic.SyncLossCause == 0
  ```

**Что изучить**:
- Методы `Run()` и `RunEMSmode()`
- Свойство `SyncLossCause` для проверки устойчивости
- Извлечение результатов из `FWDynamic`

---

### 4. Цикл определения последнего устойчивого режима

**Учебный пример**: `Примеры/пример_цикл_устойчивости.py`

**Реальный код**:
- **StabLimit**: `RIDUS/src/calc/dynamic_stability_checker.py` (строки 176-223)
  ```python
  def _check_rgm(self):
      if not rgm(rastr_win=self.rastr_win):
          while not rgm(rastr_win=self.rastr_win):
              all_at_minimum = self._adjust_generator_power(-5.0)
              if all_at_minimum:
                  break
  ```

- **StabLimit**: Бинарный поиск в `mdn_search_strategy.py`

**Что изучить**:
- Бинарный поиск (дихотомия) для быстрого нахождения границы
- Корректировка мощности генераторов
- Проверка условий завершения цикла

---

### 5. Чтение данных из Excel

**Учебный пример**: `Примеры/пример_чтение_Excel.py`

**Реальный код**:
- **DSS**: `python_dss/excel_operations/excel_operations.py`
  ```python
  class ExcelOperations:
      def get_dbl(self, row, col):
          return float(cell.value) if cell.value else 0.0
  ```

- **StabLimit**: Используется похожий подход через openpyxl

**Что изучить**:
- Чтение значений из ячеек
- Обработка разных типов данных
- Работа с несколькими листами

---

### 6. Запись результатов в Excel

**Учебный пример**: `Примеры/пример_запись_результатов.py`

**Реальный код**:
- **DSS**: `python_dss/excel_operations/excel_operations.py` (строки 44-53)
  ```python
  def set_val(self, row, col, value, wrap=True):
      cell = self._worksheet.cell(row, col)
      cell.value = value
  ```

- **StabLimit**: Похожая структура с форматированием

**Что изучить**:
- Запись данных в ячейки
- Форматирование (цвета, границы, выравнивание)
- Работа со словарями для структурирования данных

---

### 7. Работа с таблицами RasterWin

**Учебный пример**: Разбросано по примерам, но основной паттерн:

```python
table = rastr.Tables.Item("table_name")
col = table.Cols.Item("col_name")
значение = col.Z(index)  # чтение
col.SetZ(index, значение)  # запись
```

**Реальный код**:
- **DSS**: `python_dss/rastr_operations/rastr_operations.py` (строки 335-428, 430-521)
  ```python
  def get_val(self, table_name, col_name, index):
      table = self._rastr.Tables.Item(table_name)
      col = table.Cols.Item(col_name)
      return col.Z(index)
  ```

- **StabLimit**: Аналогично через `Tables()`, `Cols()`, `SetZ()`, `Z()`

**Что изучить**:
- Получение таблиц и колонок
- Чтение и запись значений
- Работа с выборками (SetSel, FindNextSel)

---

### 8. Извлечение данных из протокола

**Учебный пример**: `Примеры/пример_чтение_протокола.py`

**Реальный код**:
- **DSS**: `python_dss/rastr_operations/rastr_operations.py` (строки 912-1024)
  ```python
  def find_shunt_kz(...):
      # Парсинг сообщений протокола
      if "Величина остаточного напряжения" in description:
          prot.append(description)
  ```

- **StabLimit**: Похожий подход к обработке протоколов

**Что изучить**:
- Обработка событий OnLog
- Парсинг строк протокола
- Извлечение числовых значений из текста

---

## 📚 Рекомендуемый порядок изучения

1. **Урок 1**: Основы Python (уже готов)
2. **Урок 2**: Работа с Excel
   - Изучите: `Примеры/пример_чтение_Excel.py` и `пример_запись_результатов.py`
   - Сравните с: `DSS/python_dss/excel_operations/excel_operations.py`

3. **Урок 3**: COM-соединение
   - Изучите: `Примеры/пример_COM_соединение.py`
   - Сравните с: `DSS/python_dss/rastr_operations/rastr_operations.py` (строки 23-56)

4. **Урок 4**: RasterWin API
   - Изучите: Примеры работы с RGM и динамикой
   - Сравните с:
     - `StabLimit/RastrWinAPI/src/calculation/regim.py`
     - `StabLimit/RastrWinAPI/src/calculation/dynamic.py`

5. **Урок 5**: Расчеты и циклы
   - Изучите: `Примеры/пример_цикл_устойчивости.py`
   - Сравните с: `StabLimit/RIDUS/src/calc/dynamic_stability_checker.py`

6. **Урок 6**: Обработка результатов
   - Изучите: `Примеры/пример_чтение_протокола.py` и `пример_запись_результатов.py`
   - Сравните с реальными проектами

---

## 💡 Советы по изучению

1. **Читайте реальный код параллельно с примерами**
   - Примеры показывают упрощенную логику
   - Реальный код показывает полную реализацию

2. **Используйте примеры как отправную точку**
   - Начните с простых примеров
   - Затем переходите к реальному коду

3. **Задавайте вопросы**
   - Если что-то непонятно в реальном коде - спрашивайте
   - Я помогу разобраться

---

## 🔍 Где искать конкретные вещи

| Что нужно | Где в учебных материалах | Где в реальных проектах |
|-----------|-------------------------|------------------------|
| Подключение к RasterWin | `пример_COM_соединение.py` | `DSS/python_dss/rastr_operations/rastr_operations.py:23-56` |
| Запуск RGM | `пример_COM_соединение.py` | `StabLimit/RastrWinAPI/src/calculation/regim.py:22-61` |
| Запуск динамики | `пример_чтение_протокола.py` | `StabLimit/RastrWinAPI/src/calculation/dynamic.py:13-103` |
| Цикл поиска МДН | `пример_цикл_устойчивости.py` | `StabLimit/RIDUS/src/calc/dynamic_stability_checker.py:176-223` |
| Чтение Excel | `пример_чтение_Excel.py` | `DSS/python_dss/excel_operations/excel_operations.py` |
| Запись Excel | `пример_запись_результатов.py` | `DSS/python_dss/excel_operations/excel_operations.py:44-53` |
| Работа с таблицами | Примеры COM | `DSS/python_dss/rastr_operations/rastr_operations.py:335-521` |

